info:
  name: i18n CSV
  type: http
  seq: 3

http:
  method: GET
  url: "{{dss_url}}/rest/admin/dss/adas.dss/i18n/csv/platform_validated/true"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
