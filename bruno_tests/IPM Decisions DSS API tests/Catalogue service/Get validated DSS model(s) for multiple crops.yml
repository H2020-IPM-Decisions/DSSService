info:
  name: Get validated DSS model(s) for multiple crops
  type: http
  seq: 28

http:
  method: GET
  url: "{{dss_url}}/rest/dss/crops/SOLTU,DAUCS/platform_validated/true"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        test("Status code is 200", function () {
            expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
