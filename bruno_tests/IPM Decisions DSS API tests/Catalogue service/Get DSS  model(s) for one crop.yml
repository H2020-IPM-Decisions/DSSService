info:
  name: Get DSS  model(s) for one crop
  type: http
  seq: 25

http:
  method: GET
  url: "{{dss_url}}/rest/dss/crop/SOLTU"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        test("Status code is 200", function () {
            expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
