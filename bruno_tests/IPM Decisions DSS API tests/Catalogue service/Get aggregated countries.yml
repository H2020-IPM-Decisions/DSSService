info:
  name: Get aggregated countries
  type: http
  seq: 29

http:
  method: GET
  url: "{{dss_url}}/rest/countries"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
